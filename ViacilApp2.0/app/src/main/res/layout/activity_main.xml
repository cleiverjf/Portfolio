<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="24dp">

    <TextView
        android:id="@+id/tvTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Viacil"
        android:textSize="36sp"
        android:textStyle="bold"
        android:contentDescription="Logo de Viacil - App de traducción accesible" />

    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnCamera"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="32dp"
        android:text="Cámara en Vivo"
        android:contentDescription="Abrir cámara para traducción en tiempo real" />

    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnInterpreter"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:text="Intérprete de Frases"
        android:contentDescription="Abrir lista de frases rápidas con voz" />

    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnTranslation"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:text="Traducir Texto"
        android:contentDescription="Abrir traducción manual de texto" />

    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnSettings"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:text="Ajustes de Accesibilidad"
        android:contentDescription="Configurar opciones de accesibilidad y daltonismo" />

</androidx.constraintlayout.widget.ConstraintLayout>