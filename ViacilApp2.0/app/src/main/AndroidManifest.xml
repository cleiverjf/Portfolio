<?xml version="1.0" encoding="utf-8"?>
<manifest ... >
    <application android:theme="@style/Theme.Viacil.Splash" >
        <activity android:name=".SplashActivity" />
        <activity android:name=".MainActivity" />
        <!-- otras activities -->
    </application>
</manifest>